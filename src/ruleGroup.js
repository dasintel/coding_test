const rule = require('./rule.js');

const OP = {
  ALL: 'ALL',
  ANY: 'ANY',
}

const checkGroup = (ruleGroup, userAnswers) => {
  // Write your implementation here
}

module.exports = {
  OP,
  checkGroup
}
